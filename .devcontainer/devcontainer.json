{
  "name": "k3s-lab-nixos",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-24.04",
  "features": {
    "ghcr.io/devcontainers/features/nix:1": {
      "version": "latest",
      "extraNixConfig": "experimental-features = nix-command flakes\ntrusted-users = root vscode"
    }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "jnoortheen.nix-ide",
        "arrterian.nix-env-selector"
      ],
      "settings": {
        "nix.enableLanguageServer": true,
        "nix.serverPath": "nil",
        "nix.serverSettings": {
          "nil": {
            "formatting": {
              "command": ["nixfmt"]
            }
          }
        }
      }
    }
  },
  "postCreateCommand": "nix-env -iA nixpkgs.nil nixpkgs.nixfmt-rfc-style && echo 'Dev environment ready. Run: nix flake check'"
}
